# ============================================================================
# Horalix Halo Backend - Environment Variables
# ============================================================================

# Server Configuration
# ----------------------------------------------------------------------------
PORT=3001
NODE_ENV=development

# Frontend URL (for CORS in production)
FRONTEND_URL=http://localhost:5180

# Database
# ----------------------------------------------------------------------------
DATABASE_PATH=./horalix-backend.db

# Authentication
# ----------------------------------------------------------------------------
# IMPORTANT: Generate a secure random string for production!
# You can use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Lemon Squeezy Configuration
# ----------------------------------------------------------------------------
# Get these from your Lemon Squeezy dashboard: https://app.lemonsqueezy.com/settings/api

# API Key (Settings > API)
LEMON_SQUEEZY_API_KEY=your-lemon-squeezy-api-key

# Store ID (from your store settings)
LEMON_SQUEEZY_STORE_ID=your-store-id

# Webhook Secret (Settings > Webhooks)
LEMON_SQUEEZY_WEBHOOK_SECRET=your-webhook-secret

# Product Variant IDs
# ----------------------------------------------------------------------------
# Create products in Lemon Squeezy and paste the variant IDs here
# You can find these in the product settings under "Variants"

# Halo Plus
LEMON_SQUEEZY_PLUS_MONTHLY_VARIANT_ID=your-plus-monthly-variant-id
LEMON_SQUEEZY_PLUS_YEARLY_VARIANT_ID=your-plus-yearly-variant-id

# Halo Ultra
LEMON_SQUEEZY_ULTRA_MONTHLY_VARIANT_ID=your-ultra-monthly-variant-id
LEMON_SQUEEZY_ULTRA_YEARLY_VARIANT_ID=your-ultra-yearly-variant-id

# ============================================================================
# SETUP INSTRUCTIONS
# ============================================================================
#
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Generate a secure JWT_SECRET:
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 3. Set up Lemon Squeezy:
#    a. Create an account at https://lemonsqueezy.com
#    b. Create a store
#    c. Go to Settings > API and create an API key
#    d. Create products for Halo Plus and Halo Ultra
#    e. Create variants for monthly and yearly billing
#    f. Copy the variant IDs from the product settings
#    g. Set up webhooks at Settings > Webhooks
#       - URL: https://your-domain.com/api/webhooks/lemonsqueezy
#       - Events: All subscription events
#       - Copy the webhook secret
#
# 4. Update all the placeholder values above
#
# 5. NEVER commit the .env file to git! It's already in .gitignore
#
# ============================================================================
